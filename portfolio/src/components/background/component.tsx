//This is just made in order to get rid of the white space when the scroll is initiated
//Do not touch unless debugging Navbar
const Background = () => {
  return <>{/* <div className="bg-galaxy-gradient h-22"></div> */}</>;
};

export default Background;
